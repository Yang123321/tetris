module Gui where

import Prelude
import Thermite.DOM
import Thermite as T
import React as R
import React.DOM as R
import React.DOM.Props as RP
import ReactDOM as RDOM
import Effect (Effect)
import Effect.Console (logShow, log)


tmain :: Effect Unit
tmain = log "module package"
